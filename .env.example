# Copy to .env (git-ignored). Person C fills these in the first 30 minutes.
MINIMAX_API_KEY=
# Defaults below are usually right; override only if verify:minimax tells you to.
MINIMAX_BASE_URL=https://api.minimax.io/v1
MINIMAX_MODEL=
# Stretch (P2) only:
ELEVENLABS_API_KEY=
